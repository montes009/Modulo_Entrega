{
  "name": "modulo-operador-entregas",
  "version": "0.1.0",
  "private": true,
  "description": "Módulo de Entrega de Equipos — checklist + firma digital + PDF del acta (JS vanilla + Supabase)",
  "type": "commonjs",
  "scripts": {
    "test": "node --test",
    "check": "for f in config util rutas estados checklist firma supa plantillas entregas app; do node --check src/js/$f.js || exit 1; done"
  },
  "license": "UNLICENSED"
}
